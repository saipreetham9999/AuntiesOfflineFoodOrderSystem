meta {
  name: Delete Menu Item
  type: http
  seq: 5
}

delete {
  url: {{baseUrl}}/api/menu/{{menuItemId}}
  body: none
  auth: bearer
}

auth:bearer {
  token: {{token}}
}

headers {
  Content-Type: application/json
}
