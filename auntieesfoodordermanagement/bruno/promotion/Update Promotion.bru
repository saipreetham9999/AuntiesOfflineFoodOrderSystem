meta {
  name: Update Promotion
  type: http
  seq: 3
}

put {
  url: {{baseUrl}}/api/promotions/{{promotionId}}
  body: multipartForm
  auth: bearer
}

auth:bearer {
  token: {{token}}
}



headers {
  Content-Type: multipart/form-data
}
